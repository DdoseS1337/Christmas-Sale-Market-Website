apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: order-service
  name: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - image: 048443535585.dkr.ecr.eu-north-1.amazonaws.com/order-service:latest
          name: order-service
          env:
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: mongodb
                  key: connectionString
            - name: HTTP_PORT
              value: '3002'
            - name: RABBITMQ_URI
              valueFrom:
                secretKeyRef:
                  name: rabbitmquri
                  key: connectionLink
          ports:
            - containerPort: 3002